// The specification are a bit vague, but my implementation will handle addition, subtraction, multiplication
// and division. ex. a + b * c / d + e * f - g
// I do not see any need for a variable number of parameters for the methods in my implementation. It could be 
// useful in a simpler implementation where you only have one kind of operator at the time, ex. a - b - c - d.
// A varaible number of parameters could be implemented with the param keyword and an array.

using static System.Console;

// Local functions





// Main program

string input;
string[] tokens;

Console.WriteLine("Exit the calculator by typing \"exit\"");

while (true)
{
    // Input
    Write("Input: ");
    input = ReadLine();


    // Separate into tokens





}